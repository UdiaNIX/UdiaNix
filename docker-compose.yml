version: '3.8'
services:
  web:
    image: nginx:alpine
    volumes:
      - meu-site-volume:/usr/share/nginx/html:ro
    restart: always
    networks:
      - proxy-network

volumes:
  udianix-web-site-volume:
    external: true

networks:
  proxy-network:
    external: true
